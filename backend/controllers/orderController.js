import orderModel from "../models/order.model.js";
import userModel from "../models/user.model.js";
import Stripe from "stripe"

// placing order for user through frontend
const placeOrder = async (req,res) => {
    
}


export {placeOrder};